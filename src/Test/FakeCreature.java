package Test;

import model.Creature;

public class FakeCreature extends Creature {
    public FakeCreature(String nom, String sexe, double poids, double taille, int age, int moral) {
        super(nom, sexe, poids, taille, age, moral);
    }
}
